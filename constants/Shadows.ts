import {ViewStyle} from "react-native";

export const Shadows = {
    dp2: {
        shadowOpacity: .2,
        shadowColor: "#000000",
        shadowRadius: 3,
        elevation: 2
    }
} satisfies Record<string, ViewStyle>